Bundles installed as omphalos-bundle files are extracted to this location on
startup, unless this has previously been done or the bundle file is newer than
the last time the bundle was extracted.

The contents of the overrides folder is copied over the folders here to ensure
that changes can be safely applied without them being lost when a bundle gets
re-extracted for any reason.
